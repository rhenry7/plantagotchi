import PlantCarousel from '@renderer/components/PlantCarousel'

function App(): React.JSX.Element {
  return (
    <div className="min-h-screen bg-gray-100 py-8">
      <PlantCarousel />
    </div>
  );
}

export default App
